import React from 'react';

import './Header.css'

const Header = props => {
  return(
    <header className="main-header">
      <h1 className="logo">
        News
      </h1>
    </header>
  );
};

export default Header;